public Interface ServerInterface {
  void defaultCommandsForWriter(PrintWriter w, String input); // A method to just writes the input and flushes the writer automatically
  void run(); // The run method for the Server class
}
